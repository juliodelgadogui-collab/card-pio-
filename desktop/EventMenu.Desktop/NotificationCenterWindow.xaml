<Window x:Class="EventMenu.Desktop.NotificationCenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notificações • EventMenu" Width="1020" Height="700" MinWidth="880" MinHeight="600"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="26">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,18">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Notificações" Style="{StaticResource PageTitle}" />
                <TextBlock x:Name="SubtitleText" Text="Avisos importantes da sua operação." Style="{StaticResource PageSubtitle}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Marcar todas como lidas" Style="{StaticResource SecondaryButton}" Click="ReadAllButton_Click" />
                <Button Content="Atualizar" Style="{StaticResource SecondaryButton}" Click="RefreshButton_Click" Margin="0" />
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="1.2*" /><ColumnDefinition Width="16" /><ColumnDefinition Width="0.8*" /></Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}" Padding="14">
                <DataGrid x:Name="NotificationsGrid" IsReadOnly="True" SelectionMode="Single" SelectionChanged="NotificationsGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="" Binding="{Binding StateDisplay}" Width="65" />
                        <DataGridTextColumn Header="Tipo" Binding="{Binding PriorityDisplay}" Width="90" />
                        <DataGridTextColumn Header="Notificação" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Quando" Binding="{Binding CreatedDisplay}" Width="145" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <TextBlock x:Name="DetailPriorityText" Text="Selecione uma notificação" Style="{StaticResource FieldLabel}" />
                    <TextBlock Grid.Row="1" x:Name="DetailTitleText" Text="" FontSize="20" FontWeight="SemiBold" Margin="0,8,0,12" TextWrapping="Wrap" />
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="DetailMessageText" Text="Escolha um aviso à esquerda para ver os detalhes." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" LineHeight="22" />
                    </ScrollViewer>
                    <StackPanel Grid.Row="3" Margin="0,18,0,0">
                        <TextBlock x:Name="DetailTimeText" Foreground="{DynamicResource MutedBrush}" FontSize="11" Margin="0,0,0,10" />
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="ReadButton" Content="Marcar como lida" Click="ReadButton_Click" IsEnabled="False" />
                            <Button x:Name="OpenRelatedButton" Content="Abrir relacionado" Style="{StaticResource SecondaryButton}" Click="OpenRelatedButton_Click" Visibility="Collapsed" Margin="0" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <TextBlock x:Name="StatusText" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center" />
            <Button Content="Fechar" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Click="CloseButton_Click" Margin="0" />
        </Grid>
    </Grid>
</Window>